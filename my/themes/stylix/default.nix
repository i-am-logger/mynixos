# Stylix static theming - legacy, disabled by default
# Options are defined in my/themes/options.nix under themes.stylix
# Theme loading is handled by mkSystem.nix when my.themes.stylix.type = "stylix"
# This module is intentionally empty as stylix configuration is injected externally
_:

{ }
